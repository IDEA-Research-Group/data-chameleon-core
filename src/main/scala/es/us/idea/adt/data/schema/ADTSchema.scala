package es.us.idea.adt.data.schema

/** An ADTSchema represents the schema of a Data object.
  *
  * ADTSchema object wrap the main Apache Spark classes that model the DataFrame schemas: DataType and StructField.
  *
  */
trait ADTSchema extends Serializable